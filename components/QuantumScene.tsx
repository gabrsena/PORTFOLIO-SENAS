/**
 * @license
 * SPDX-License-Identifier: Apache-2.0
*/

import React from 'react';

// This component has been replaced by a background video in App.tsx.
// Kept as a placeholder to avoid build errors if referenced elsewhere, 
// though it is no longer used in the main application.
export const FlowerScene: React.FC = () => {
  return null;
};